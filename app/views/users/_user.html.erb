<li>
  <%= gravatar_for user, size: Settings.size_50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t("users.delete.link"), user, data: { "turbo-method": :delete,turbo_confirm: t("users.delete.are_you_sure") }%>
  <% end %>
</li>
